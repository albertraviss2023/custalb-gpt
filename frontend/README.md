# Frontend App

React + Vite chat UI for Personal GOI.

## Features

- Chat interface with streaming responses
- Sidebar chat management
- Per-chat model selector
- API integration with backend gateway

## Run Locally

```bash
npm install
npm run dev
```

Default API base is `/api` and Vite dev proxy rewrites to `http://localhost:8000`.